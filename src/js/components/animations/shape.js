// const ACTIVE = 'active';
const SHAPE = document.getElementById('mainFormShape');

const shapeResize = (() => {
   const shapefunction = function elem_in_visible_area(SHAPE) {
      
  const init = () => {
 
  };

  return {
    init,
  };
})();

export default shapeResize;